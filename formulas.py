#!/usr/bin/python

import sys, os, parsers
import numpy

#inverse term frequency

#inverse document frequency

#Probability generator
#Cond probablitiy of term in the topic
def probability(quest, qNum, freqMatrix, topicProb)
  #Use the term frequency matrix to generate a probability/topic dictionary
  print topicProb
  
  for term in quest:
    tProb=(frequency of terms in the topic)

    
    
    
  
    




#Calculate probability of topic per tital list of questions
def topicProb(topicDict):
  topic_calc = {}
  for i in range(1,42):
    count = 0
    for topic in topicDict:
      if topic[i] == i
        count += 1
    topic_calc{i} = double(count/41)
  return topic_calc

#Cond probablitiy of the 
